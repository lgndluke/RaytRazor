#version 330 core

in vec3 fragColor;  // color passed from vertex shader
out vec4 FragColor; // final color output

//Base-FragmentShader without Raytracer atm
void main() {
    FragColor = vec4(fragColor, 1.0); // set final fragment color
}
